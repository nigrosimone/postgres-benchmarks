{
  "name": "pg-bench",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bench": "node --expose-gc --allow-natives-syntax index.js --iterations=50000",
    "docker-build": "docker-compose build",
    "docker-up": "docker-compose up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mitata": "^1.0.34",
    "pg": "^8.16.3",
    "pg-native": "^3.5.2",
    "postgres": "^3.4.7"
  }
}