{
  "name": "postgres-benchmarks",
  "version": "1.0.0",
  "description": "A benchmark focusing on the performance of Postgres client libraries for Node.js, brianc/node-postgres VS porsager/postgres",
  "keywords": ["nodejs", "javascript", "benchmark", "postgresql", "node-js", "pg", "postgres", "pg-native", "node", "postgres.js"],
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bench": "node --expose-gc --allow-natives-syntax index.ts --iterations=50000",
    "docker-build": "docker-compose build",
    "docker-up": "docker-compose up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mitata": "^1.0.34",
    "pg": "latest",
    "pg-native": "latest",
    "postgres": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/pg": "latest"
  }
}
